{
  "name": "xrpl-tx-export",
  "version": "1.0.2",
  "description": "Fetch XRPL transactions to CSV format",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify -r .:xrpl-tx-export -o dist/xrpl-tx-export.js"
  },
  "author": "Wietse Wind <xrpl@wietse.com>",
  "license": "MIT",
  "dependencies": {
    "ripple-lib": "^1.10.0",
    "ripple-lib-transactionparser": "^0.8.2",
    "xrpl-client": "^1.9.2"
  },
  "devDependencies": {
    "browserify": "^17.0.0"
  }
}
